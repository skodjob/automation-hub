apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: twitter-app-copy-secret-task
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  workspaces:
    - name: task-ws
  steps:
    - name: tealc-clone
      image: alpine/git:v2.30.2
      script: |
        export CURRENT_DIR=$(workspaces.task-ws.path)/tealc
        git clone https://github.com/ExcelentProject/tealc.git $CURRENT_DIR
    - name: images-update
      env:
        - name: OC_USERNAME
          valueFrom:
            secretKeyRef:
              name: tealc-worker-tekton
              key: username
        - name: OC_PASSWORD
          valueFrom:
            secretKeyRef:
              name: tealc-worker-tekton
              key: password
        - name: OC_API_URL
          valueFrom:
            secretKeyRef:
              name: tealc-worker-tekton
              key: server
      image: quay.io/tealc/apophis:latest
      script: |
        oc login -u $OC_USERNAME -p $OC_PASSWORD $OC_API_URL --insecure-skip-tls-verify=true
        $(workspaces.task-ws.path)/tealc/scripts/copy-secrets.sh
---
apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: twitter-app-copy-secret-pipeline
  annotations:
    argocd.argoproj.io/hook: PreSync
spec:
  workspaces:
    - name: pipeline-ws
  tasks:
    - name: twitter-app-copy-secret
      taskRef:
        name: twitter-app-copy-secret-task
      workspaces:
        - name: task-ws
          workspace: pipeline-ws
