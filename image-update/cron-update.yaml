apiVersion: batch/v1
kind: CronJob
metadata:
  name: image-update-cron
spec:
  schedule: "*/1 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: image-updater
            image: quay.io/obabec/image-updater:latest
            imagePullPolicy: IfNotPresent
            env:
            - name: CURRENT_DEPLOYMENT_REPO
              value: "https://github.com/ExcelentProject/test-image-updater.git"
            - name: YAML_BUNDLE_PATH
              value: "./"
            - name: TARGET_ORG_REPO
              value: "quay.io/strimzi"
            - name: BRANCH
              value: tealc-ci-test
            - name: GITHUB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: github-secret
                  key: USERNAME
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-secret
                  key: TOKEN
          restartPolicy: OnFailure