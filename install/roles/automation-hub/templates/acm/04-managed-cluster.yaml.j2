apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  labels:
    cloud: auto-detect
  vendor: auto-detect
  name: {{ cluster.name }}
  namespace: {{ cluster.name }}
spec:
  hubAcceptsClient: true

