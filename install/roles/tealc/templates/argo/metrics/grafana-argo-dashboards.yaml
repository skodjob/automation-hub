apiVersion: integreatly.org/v1alpha1
kind: GrafanaDashboard
metadata:
  labels:
    app: tealc
    monitoring-key: middleware
    project: tealc
  name: argo-monitoring
spec:
  datasources:
    - inputName: "datasource"
      datasourceName: "tealc"
  configMapRef:
    name: grafana-dashboard-argo-config
    key: argo
